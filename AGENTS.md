# AGENTS.md

Project instructions for Codex / ChatGPT and other AGENTS.md-aware agents.

The full, canonical instructions for this repo live in **`CLAUDE.md`** (same directory) —
read it first. This file is only a pointer so AGENTS.md-aware tools load the same context;
keep the real content in `CLAUDE.md`, never duplicate it here.

Cross-repo contracts (version registry + parity-lock index): `../docs/CONTRACTS.md`.
